{
  "_comment": "OBSOLETE as of the Worker-backed update feed. The app no longer carries a GitHub token: electron/main.cjs points electron-updater at https://digital-twin.urology.edu.eu.org/api/updates, and the Worker holds RELEASES_READ_PAT as a secret (npx wrangler secret put RELEASES_READ_PAT). Kept only so an older checkout that still reads update-auth.json has something to copy; nothing in the current build reads this file."
}
